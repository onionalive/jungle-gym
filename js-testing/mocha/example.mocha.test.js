const expect = require('chai').expect;

// Test suite
describe('Mocha', () => {
	// Test spec (unit test)
	it('should run our test to be okay', () => {
		expect(true).to.be.ok;
	});

	it('Hello from Mocha and Chai, Jungle Gym!');
});